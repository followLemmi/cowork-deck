# Group C — «Наблюдаемость и отличия» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Наблюдаемость расхода токенов (на сессию + сводка), git-контекст на плитке, персист окна/активного пространства, и плавающая пилюля «N ждут ввода» поверх окон.

**Architecture:** Backend-команды Rust отдают git-статус и сумму токенов (из transcript claude, найденного по glob `<session-id>.jsonl`); фронтенд опрашивает их поллингом (5с) и рисует бейджи. Персист окна — плагин `tauri-plugin-window-state`; активное пространство — `ui_state.json`. Пилюля — второе окно Tauri (transparent, always-on-top), событийно связанное с главным.

**Tech Stack:** Tauri v2, Rust, ванильный TypeScript + xterm.js, vitest (+jsdom), cargo test. Новая зависимость: `tauri-plugin-window-state`.

## Global Constraints

- Tauri v2, ядро Rust + ванильный TypeScript + xterm.js. Только ванильный TS, без фреймворков.
- Целевой расход памяти < 100 МБ; только лёгкие зависимости.
- Тёмная тема, дизайн-токены из `styles.css` (`:root`).
- **Анимации только `transform`/`opacity`; НИКОГДА `box-shadow`/`filter` с `infinite` (ADR-008).**
- Строки интерфейса — на русском.
- Тесты: `npm test` (vitest run), `cargo test --manifest-path src-tauri/Cargo.toml`. На старте ветки: 64 vitest / 17 cargo зелёные. `tsc --noEmit` чист.
- Conventional Commits. Ветка `feat/group-c` (от `main`, merge `6dd04f1`). Спек: `docs/superpowers/specs/2026-07-24-group-c-design.md`.

**Проверенные факты (спайк):** transcript = `~/.claude/projects/*/<session-id>.jsonl`; строки `type:"assistant"` несут `message.usage.{input_tokens,output_tokens,cache_creation_input_tokens,cache_read_input_tokens}`.

---

## Task 1: window-state плагин (персист размера/позиции окна)

**Files:**
- Modify: `src-tauri/Cargo.toml` (`[dependencies]`)
- Modify: `src-tauri/src/main.rs` (регистрация плагина)
- Modify: `src-tauri/capabilities/default.json` (разрешение)

- [ ] **Step 1: Добавить зависимость**

В `src-tauri/Cargo.toml`, в `[dependencies]`, добавить:
```toml
tauri-plugin-window-state = "2"
```

- [ ] **Step 2: Зарегистрировать плагин**

В `src-tauri/src/main.rs`, в цепочке `tauri::Builder::default()`, после `.plugin(tauri_plugin_dialog::init())` добавить строку:
```rust
        .plugin(tauri_plugin_window_state::Builder::default().build())
```

- [ ] **Step 3: Разрешение в capability**

В `src-tauri/capabilities/default.json`, в массив `"permissions"`, добавить `"window-state:default"`.

- [ ] **Step 4: Сборка**

Run: `cargo build --manifest-path src-tauri/Cargo.toml`
Expected: компилируется без ошибок (плагин подтянут).

- [ ] **Step 5: Commit**

```bash
git add src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/src/main.rs src-tauri/capabilities/default.json
git commit -m "feat: persist window size/position via tauri-plugin-window-state"
```

---

## Task 2: Rust — `UiState` + персист активного пространства (store + команды)

**Files:**
- Modify: `src-tauri/src/model.rs` (тип `UiState`)
- Modify: `src-tauri/src/store.rs` (импорт + методы `ui_state`/`save_ui_state`, tests)
- Modify: `src-tauri/src/commands.rs` (импорт + команды)
- Modify: `src-tauri/src/main.rs` (регистрация команд)

**Interfaces:**
- Produces: `model::UiState { active_workspace_id: Option<String> (serde "activeWorkspaceId") }`
- Produces: `Store::ui_state() -> UiState`, `Store::save_ui_state(&UiState) -> io::Result<()>`
- Produces: команды `load_ui_state() -> UiState`, `save_ui_state(ui: UiState) -> Result<(), String>`

- [ ] **Step 1: Тест round-trip (падает)**

В `mod tests` в `src-tauri/src/store.rs` (импорт `use crate::model::UiState;` рядом с прочими) добавить:
```rust
    #[test]
    fn ui_state_round_trips_and_defaults_empty() {
        let s = Store::new(tmp());
        assert_eq!(s.ui_state(), UiState::default()); // NotFound -> default (None)
        let st = UiState { active_workspace_id: Some("w-1".into()) };
        s.save_ui_state(&st).unwrap();
        assert_eq!(Store::new(s.dir.clone()).ui_state(), st);
    }
```

- [ ] **Step 2: Запуск — падает**

Run: `cargo test --manifest-path src-tauri/Cargo.toml ui_state_round_trips`
Expected: FAIL (тип/методы не существуют).

- [ ] **Step 3: `UiState` в `model.rs`**

После `SessionEntry` добавить:
```rust
/// Небольшое UI-состояние, переживающее перезапуск (пока — активное пространство).
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Default)]
pub struct UiState {
    #[serde(rename = "activeWorkspaceId")]
    pub active_workspace_id: Option<String>,
}
```

- [ ] **Step 4: Методы в `store.rs`**

Добавить `UiState` в импорт модели вверху файла: `use crate::model::{SessionEntry, Skill, UiState, Workspace};`. В `impl Store` добавить:
```rust
    fn ui_path(&self) -> PathBuf { self.dir.join("ui_state.json") }
    pub fn ui_state(&self) -> UiState {
        match std::fs::read_to_string(self.ui_path()) {
            Ok(s) => serde_json::from_str(&s).unwrap_or_default(),
            Err(_) => UiState::default(),
        }
    }
    pub fn save_ui_state(&self, st: &UiState) -> std::io::Result<()> {
        let json = serde_json::to_string_pretty(st)
            .map_err(|e| std::io::Error::new(std::io::ErrorKind::Other, e))?;
        std::fs::write(self.ui_path(), json)
    }
```

- [ ] **Step 5: Команды в `commands.rs` + регистрация**

Добавить `UiState` в импорт модели в `commands.rs`. После `save_layout` добавить:
```rust
#[tauri::command]
pub fn load_ui_state(state: State<AppState>) -> UiState {
    state.store.lock().unwrap().ui_state()
}

#[tauri::command]
pub fn save_ui_state(state: State<AppState>, ui: UiState) -> Result<(), String> {
    state.store.lock().unwrap().save_ui_state(&ui).map_err(|e| e.to_string())
}
```
В `main.rs` в `generate_handler![...]` добавить `commands::load_ui_state,` и `commands::save_ui_state,`.

- [ ] **Step 6: Тесты зелёные**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS (+1 тест).

- [ ] **Step 7: Commit**

```bash
git add src-tauri/src/model.rs src-tauri/src/store.rs src-tauri/src/commands.rs src-tauri/src/main.rs
git commit -m "feat: persist active workspace in ui_state.json (load/save_ui_state)"
```

---

## Task 3: TS — восстановление активного пространства

**Files:**
- Modify: `src/ipc.ts` (тип `UiState` + `loadUiState`/`saveUiState`)
- Modify: `src/workspaces.ts` (сохранять при select, восстанавливать при load)
- Create: `tests/workspaces.test.ts` — если файла нет; иначе Modify (добавить кейсы)

**Interfaces:**
- Consumes: команды `load_ui_state`/`save_ui_state` (Task 2)
- Produces: `UiState { activeWorkspaceId: string | null }`; `loadUiState()`, `saveUiState(ui)`

- [ ] **Step 1: Тесты (падают)**

Добавить/создать `tests/workspaces.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect, vi, beforeEach } from "vitest";

const { listWorkspacesMock, loadUiStateMock, saveUiStateMock } = vi.hoisted(() => ({
  listWorkspacesMock: vi.fn(), loadUiStateMock: vi.fn(), saveUiStateMock: vi.fn(),
}));
vi.mock("../src/ipc", () => ({
  listWorkspaces: listWorkspacesMock, saveWorkspace: vi.fn(), removeWorkspace: vi.fn(),
  loadUiState: loadUiStateMock, saveUiState: saveUiStateMock,
}));
vi.mock("../src/modal", () => ({ confirmModal: vi.fn() }));
vi.mock("../src/forms", () => ({ workspaceForm: vi.fn() }));

import { WorkspacesPanel } from "../src/workspaces";

beforeEach(() => { vi.clearAllMocks(); document.body.innerHTML = ""; saveUiStateMock.mockResolvedValue(undefined); });

describe("WorkspacesPanel restore", () => {
  const items = [{ id: "a", name: "A", path: "/a", color: "#111" }, { id: "b", name: "B", path: "/b", color: "#222" }];
  it("selects the saved active workspace when present", async () => {
    listWorkspacesMock.mockResolvedValue(items);
    loadUiStateMock.mockResolvedValue({ activeWorkspaceId: "b" });
    const selected: string[] = [];
    const p = new WorkspacesPanel(document.createElement("div"), (ws) => selected.push(ws.id));
    await p.load();
    expect(p.active?.id).toBe("b");
    expect(selected).toContain("b");
  });
  it("falls back to first when saved id is absent/unknown", async () => {
    listWorkspacesMock.mockResolvedValue(items);
    loadUiStateMock.mockResolvedValue({ activeWorkspaceId: "gone" });
    const p = new WorkspacesPanel(document.createElement("div"), () => {});
    await p.load();
    expect(p.active?.id).toBe("a");
  });
});
```

- [ ] **Step 2: Запуск — падает**

Run: `npm test -- workspaces`
Expected: FAIL (`loadUiState` не экспортирован / нет восстановления).

- [ ] **Step 3: IPC в `src/ipc.ts`**

```ts
export interface UiState { activeWorkspaceId: string | null; }
export const loadUiState = () => invoke<UiState>("load_ui_state");
export const saveUiState = (ui: UiState) => invoke<void>("save_ui_state", { ui });
```

- [ ] **Step 4: Восстановление в `src/workspaces.ts`**

Добавить в импорт из `./ipc`: `loadUiState, saveUiState, type UiState`. Заменить `load()` и `select()`:
```ts
  async load() {
    this.items = await listWorkspaces();
    if (!this.activeId && this.items.length) {
      const saved = (await loadUiState()).activeWorkspaceId;
      const pick = saved && this.items.some((w) => w.id === saved) ? saved : this.items[0].id;
      this.select(pick);
    }
    this.render();
  }

  private select(id: string) {
    this.activeId = id;
    void saveUiState({ activeWorkspaceId: id });
    const ws = this.active;
    if (ws) this.onSelect(ws);
    this.render();
  }
```

- [ ] **Step 5: Тесты и типы**

Run: `npm test -- workspaces` → PASS
Run: `npm test` → PASS (полный)
Run: `npx tsc --noEmit` → чисто

- [ ] **Step 6: Commit**

```bash
git add src/ipc.ts src/workspaces.ts tests/workspaces.test.ts
git commit -m "feat: restore active workspace on startup, persist on select"
```

---

## Task 4: Rust — `git_status` команда

**Files:**
- Modify: `src-tauri/src/model.rs` (тип `GitStatus`)
- Modify: `src-tauri/src/commands.rs` (команда + tests)
- Modify: `src-tauri/src/main.rs` (регистрация)

**Interfaces:**
- Produces: `model::GitStatus { branch: Option<String>, dirty: bool }`
- Produces: команда `git_status(cwd: String) -> GitStatus`

- [ ] **Step 1: Тест на temp-репо (падает)**

В `mod tests` в `commands.rs` добавить:
```rust
    #[test]
    fn git_status_reports_branch_and_dirty() {
        use std::process::Command;
        let dir = std::env::temp_dir().join(format!("cowork-git-{}-{:?}", std::process::id(), std::time::SystemTime::now()));
        std::fs::create_dir_all(&dir).unwrap();
        let cwd = dir.to_str().unwrap();
        let run = |args: &[&str]| { Command::new("git").arg("-C").arg(cwd).args(args).output().unwrap(); };
        run(&["init"]);
        run(&["config", "user.email", "t@t"]);
        run(&["config", "user.name", "t"]);
        std::fs::write(dir.join("a.txt"), "hi").unwrap();
        run(&["add", "."]);
        run(&["commit", "-m", "init"]);

        let clean = git_status(cwd.to_string());
        assert!(clean.branch.is_some(), "committed repo must report a branch");
        assert!(!clean.dirty, "just-committed repo is clean");

        std::fs::write(dir.join("b.txt"), "new").unwrap(); // untracked → dirty
        let dirty = git_status(cwd.to_string());
        assert!(dirty.dirty, "untracked file makes it dirty");

        let non_repo = git_status(std::env::temp_dir().to_str().unwrap().to_string());
        // temp_dir itself is not a repo (usually); branch None. Tolerate either but dirty must be false when branch is None.
        if non_repo.branch.is_none() { assert!(!non_repo.dirty); }
    }
```

- [ ] **Step 2: Запуск — падает**

Run: `cargo test --manifest-path src-tauri/Cargo.toml git_status_reports`
Expected: FAIL (нет `git_status`).

- [ ] **Step 3: `GitStatus` в `model.rs`**

После `UiState` добавить:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct GitStatus {
    pub branch: Option<String>,
    pub dirty: bool,
}
```

- [ ] **Step 4: Команда в `commands.rs`**

Добавить `GitStatus` в импорт модели. Добавить команду:
```rust
#[tauri::command]
pub fn git_status(cwd: String) -> GitStatus {
    use std::process::Command;
    let branch = Command::new("git")
        .arg("-C").arg(&cwd)
        .args(["rev-parse", "--abbrev-ref", "HEAD"])
        .output().ok()
        .filter(|o| o.status.success())
        .map(|o| String::from_utf8_lossy(&o.stdout).trim().to_string())
        .filter(|s| !s.is_empty() && s != "HEAD");
    let dirty = branch.is_some()
        && Command::new("git")
            .arg("-C").arg(&cwd)
            .args(["status", "--porcelain"])
            .output().ok()
            .map(|o| !o.stdout.is_empty())
            .unwrap_or(false);
    GitStatus { branch, dirty }
}
```
Зарегистрировать `commands::git_status,` в `main.rs`.

- [ ] **Step 5: Тесты зелёные**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/model.rs src-tauri/src/commands.rs src-tauri/src/main.rs
git commit -m "feat: git_status command (branch + dirty) for tile badge"
```

---

## Task 5: Rust — `session_tokens` команда (сумма usage из transcript)

**Files:**
- Modify: `src-tauri/src/model.rs` (тип `TokenUsage`)
- Modify: `src-tauri/src/commands.rs` (pure `sum_usage_lines` + `find_transcript` + команда + tests)
- Modify: `src-tauri/src/main.rs` (регистрация)

**Interfaces:**
- Produces: `model::TokenUsage { input, output, cache_creation (serde "cacheCreation"), cache_read (serde "cacheRead") }` (все `u64`)
- Produces: `sum_usage_lines(content: &str) -> TokenUsage` (pure, тестируемо)
- Produces: команда `session_tokens(session_id: String) -> TokenUsage`

- [ ] **Step 1: Тест суммирования (падает)**

В `mod tests` в `commands.rs`:
```rust
    #[test]
    fn sum_usage_lines_adds_assistant_usage_only() {
        let content = concat!(
            r#"{"type":"user","message":{"role":"user","content":"hi"}}"#, "\n",
            r#"{"type":"assistant","message":{"usage":{"input_tokens":10,"output_tokens":5,"cache_creation_input_tokens":100,"cache_read_input_tokens":200}}}"#, "\n",
            "not json at all", "\n",
            r#"{"type":"assistant","message":{"usage":{"input_tokens":3,"output_tokens":7,"cache_creation_input_tokens":0,"cache_read_input_tokens":50}}}"#, "\n",
        );
        let u = sum_usage_lines(content);
        assert_eq!(u.input, 13);
        assert_eq!(u.output, 12);
        assert_eq!(u.cache_creation, 100);
        assert_eq!(u.cache_read, 250);
    }

    #[test]
    fn sum_usage_lines_empty_is_zero() {
        assert_eq!(sum_usage_lines(""), TokenUsage::default());
    }
```

- [ ] **Step 2: Запуск — падает**

Run: `cargo test --manifest-path src-tauri/Cargo.toml sum_usage_lines`
Expected: FAIL.

- [ ] **Step 3: `TokenUsage` в `model.rs`**

После `GitStatus` добавить:
```rust
#[derive(Debug, Clone, Copy, Serialize, Deserialize, PartialEq, Default)]
pub struct TokenUsage {
    pub input: u64,
    pub output: u64,
    #[serde(rename = "cacheCreation")]
    pub cache_creation: u64,
    #[serde(rename = "cacheRead")]
    pub cache_read: u64,
}
```

- [ ] **Step 4: Реализация в `commands.rs`**

Добавить `TokenUsage` в импорт модели. Добавить:
```rust
/// Sum `message.usage.*` token counts across all JSONL lines. Tolerant of
/// non-JSON lines and lines without usage (user messages, meta).
pub fn sum_usage_lines(content: &str) -> TokenUsage {
    let mut u = TokenUsage::default();
    for line in content.lines() {
        let v: serde_json::Value = match serde_json::from_str(line) {
            Ok(v) => v,
            Err(_) => continue,
        };
        let usage = &v["message"]["usage"];
        if usage.is_object() {
            u.input += usage["input_tokens"].as_u64().unwrap_or(0);
            u.output += usage["output_tokens"].as_u64().unwrap_or(0);
            u.cache_creation += usage["cache_creation_input_tokens"].as_u64().unwrap_or(0);
            u.cache_read += usage["cache_read_input_tokens"].as_u64().unwrap_or(0);
        }
    }
    u
}

/// Locate the transcript file `<session_id>.jsonl` under any project dir.
fn find_transcript(session_id: &str) -> Option<std::path::PathBuf> {
    let home = std::env::var_os("HOME")?;
    let base = std::path::PathBuf::from(home).join(".claude/projects");
    let target = format!("{session_id}.jsonl");
    for entry in std::fs::read_dir(&base).ok()? {
        let dir = entry.ok()?.path();
        let f = dir.join(&target);
        if f.is_file() {
            return Some(f);
        }
    }
    None
}

#[tauri::command]
pub fn session_tokens(session_id: String) -> TokenUsage {
    match find_transcript(&session_id) {
        Some(path) => std::fs::read_to_string(path)
            .map(|c| sum_usage_lines(&c))
            .unwrap_or_default(),
        None => TokenUsage::default(),
    }
}
```
Зарегистрировать `commands::session_tokens,` в `main.rs`.

- [ ] **Step 5: Тесты зелёные**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS (+2 теста).

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/model.rs src-tauri/src/commands.rs src-tauri/src/main.rs
git commit -m "feat: session_tokens command (sum usage from claude transcript)"
```

---

## Task 6: TS — слой наблюдаемости (IPC + чистые хелперы)

**Files:**
- Modify: `src/ipc.ts` (типы `GitStatus`/`TokenUsage` + `gitStatus`/`sessionTokens`)
- Create: `src/observability.ts` (чистые функции)
- Create: `tests/observability.test.ts`

**Interfaces:**
- Consumes: команды `git_status`/`session_tokens` (Tasks 4/5)
- Produces: `GitStatus { branch: string | null; dirty: boolean }`, `TokenUsage { input; output; cacheCreation; cacheRead }`
- Produces: `formatTokens(n: number): string`, `sumUsage(list: TokenUsage[]): TokenUsage`, `uniqueCwds(items: {cwd: string}[]): string[]`

- [ ] **Step 1: Тесты (падают)**

`tests/observability.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { formatTokens, sumUsage, uniqueCwds } from "../src/observability";

describe("formatTokens", () => {
  it("passes through small numbers", () => { expect(formatTokens(0)).toBe("0"); expect(formatTokens(999)).toBe("999"); });
  it("uses k for thousands", () => { expect(formatTokens(1200)).toBe("1.2k"); expect(formatTokens(45230)).toBe("45.2k"); });
  it("uses M for millions", () => { expect(formatTokens(2_500_000)).toBe("2.5M"); });
});

describe("sumUsage", () => {
  it("sums field-wise", () => {
    const r = sumUsage([
      { input: 1, output: 2, cacheCreation: 3, cacheRead: 4 },
      { input: 10, output: 20, cacheCreation: 30, cacheRead: 40 },
    ]);
    expect(r).toEqual({ input: 11, output: 22, cacheCreation: 33, cacheRead: 44 });
  });
  it("empty is all-zero", () => { expect(sumUsage([])).toEqual({ input: 0, output: 0, cacheCreation: 0, cacheRead: 0 }); });
});

describe("uniqueCwds", () => {
  it("dedupes by cwd preserving order", () => {
    expect(uniqueCwds([{ cwd: "/a" }, { cwd: "/b" }, { cwd: "/a" }])).toEqual(["/a", "/b"]);
  });
});
```

- [ ] **Step 2: Запуск — падает**

Run: `npm test -- observability`
Expected: FAIL.

- [ ] **Step 3: `src/observability.ts`**

```ts
import type { TokenUsage } from "./ipc";

export function formatTokens(n: number): string {
  if (n >= 1_000_000) return `${(n / 1_000_000).toFixed(1)}M`;
  if (n >= 1_000) return `${(n / 1_000).toFixed(1)}k`;
  return String(n);
}

export function sumUsage(list: TokenUsage[]): TokenUsage {
  return list.reduce(
    (a, u) => ({
      input: a.input + u.input,
      output: a.output + u.output,
      cacheCreation: a.cacheCreation + u.cacheCreation,
      cacheRead: a.cacheRead + u.cacheRead,
    }),
    { input: 0, output: 0, cacheCreation: 0, cacheRead: 0 },
  );
}

export function uniqueCwds(items: { cwd: string }[]): string[] {
  const seen = new Set<string>();
  const out: string[] = [];
  for (const { cwd } of items) if (!seen.has(cwd)) { seen.add(cwd); out.push(cwd); }
  return out;
}
```

- [ ] **Step 4: IPC в `src/ipc.ts`**

```ts
export interface GitStatus { branch: string | null; dirty: boolean; }
export interface TokenUsage { input: number; output: number; cacheCreation: number; cacheRead: number; }
export const gitStatus = (cwd: string) => invoke<GitStatus>("git_status", { cwd });
export const sessionTokens = (sessionId: string) => invoke<TokenUsage>("session_tokens", { sessionId });
```

- [ ] **Step 5: Тесты и типы**

Run: `npm test -- observability` → PASS; `npm test` → PASS; `npx tsc --noEmit` → чисто

- [ ] **Step 6: Commit**

```bash
git add src/ipc.ts src/observability.ts tests/observability.test.ts
git commit -m "feat: observability IPC (git_status/session_tokens) + pure helpers"
```

---

## Task 7: TS — бейджи плиток (git + токены) + сводка + поллинг

**Files:**
- Modify: `src/sessions.ts` (`Tile` +поля бейджей; `spawnTile` строит бейджи; поллинг-цикл; сайдбар-сводка)
- Modify: `src/styles.css` (стили бейджей)

**Interfaces:**
- Consumes: `gitStatus`, `sessionTokens` (Task 6 ipc), `formatTokens`, `sumUsage`, `uniqueCwds` (Task 6)

- [ ] **Step 1: Поля бейджей в `Tile` и построение в `spawnTile`**

В интерфейс `Tile` добавить: `gitBadge: HTMLElement; tokenBadge: HTMLElement;`.
В `spawnTile`, при сборке `head` (после `title`, до `label`), создать бейджи:
```ts
    const gitBadge = document.createElement("span");
    gitBadge.className = "tile-git hidden";
    const tokenBadge = document.createElement("span");
    tokenBadge.className = "tile-tokens hidden";
```
и вставить их в head: `head.append(title, gitBadge, tokenBadge, label, clearBtn, close);` (заменить существующий `head.append(title, label, clearBtn, close);`). Добавить `gitBadge, tokenBadge` в литерал `tile: Tile = { … }`.

- [ ] **Step 2: Импорты и поллинг в `Deck`**

Добавить импорты в начале `src/sessions.ts`:
```ts
import { gitStatus, sessionTokens, type TokenUsage } from "./ipc";
import { formatTokens, sumUsage, uniqueCwds } from "./observability";
```
Добавить поля и методы в класс `Deck`:
```ts
  private pollTimer: ReturnType<typeof setInterval> | null = null;
  private usage = new Map<string, TokenUsage>();

  private startPolling() {
    if (this.pollTimer !== null) return;
    void this.pollOnce();
    this.pollTimer = setInterval(() => void this.pollOnce(), 5000);
  }
  private stopPolling() {
    if (this.pollTimer !== null) { clearInterval(this.pollTimer); this.pollTimer = null; }
  }

  private async pollOnce() {
    const tiles = [...this.tiles.values()];
    if (tiles.length === 0) { this.stopPolling(); return; }
    // git: один вызов на уникальный cwd
    const cwds = uniqueCwds(tiles.map((t) => ({ cwd: t.workspacePath })));
    const gitByCwd = new Map<string, { branch: string | null; dirty: boolean }>();
    await Promise.all(cwds.map(async (cwd) => { gitByCwd.set(cwd, await gitStatus(cwd)); }));
    for (const t of tiles) {
      const g = gitByCwd.get(t.workspacePath);
      if (g && g.branch) {
        t.gitBadge.textContent = `⎇ ${g.branch}${g.dirty ? " •" : ""}`;
        t.gitBadge.classList.remove("hidden");
      } else {
        t.gitBadge.classList.add("hidden");
      }
    }
    // tokens: по сессии
    await Promise.all(tiles.map(async (t) => {
      const u = await sessionTokens(t.session);
      this.usage.set(t.session, u);
      t.tokenBadge.textContent = `↑${formatTokens(u.input)} ↓${formatTokens(u.output)}`;
      t.tokenBadge.title = `cache: +${formatTokens(u.cacheCreation)} / ${formatTokens(u.cacheRead)} прочитано`;
      t.tokenBadge.classList.remove("hidden");
    }));
    this.renderList();
  }
```
Вызвать `this.startPolling();` в `spawnTile` после `this.tiles.set(session, tile);`. В `remove`, после `this.tiles.delete(session)`, добавить: `this.usage.delete(session); if (this.tiles.size === 0) this.stopPolling();`.

- [ ] **Step 2b: Сводка токенов в сайдбаре**

В методе `renderList`, после установки заголовка (`this.listEl.innerHTML = ...`), добавить строку сводки, если есть данные:
```ts
    const total = sumUsage([...this.usage.values()]);
    if (this.usage.size > 0) {
      const sum = document.createElement("div");
      sum.className = "sess-tokens-sum";
      sum.textContent = `Σ токенов · ↑${formatTokens(total.input)} ↓${formatTokens(total.output)}`;
      this.listEl.appendChild(sum);
    }
```

- [ ] **Step 3: Стили в `styles.css`**

```css
.tile-git, .tile-tokens {
  font-size: var(--fs-xs); color: var(--fg-subtle); white-space: nowrap;
  padding: 0 6px; border-radius: 999px; background: var(--bg-app);
}
.tile-git.hidden, .tile-tokens.hidden { display: none; }
.sess-tokens-sum { font-size: var(--fs-xs); color: var(--fg-subtle); margin: 4px 0 8px; }
```
(Сверить имена токенов в `:root`; при отсутствии `--fg-subtle`/`--bg-app` — взять существующие аналоги.)

- [ ] **Step 4: Тесты и типы**

Run: `npm test` → PASS (регрессий нет; чистые хелперы покрыты Task 6)
Run: `npx tsc --noEmit` → чисто

- [ ] **Step 5: Ручная проверка (десктоп, чеклист)**

Открыть сессию в git-репо → в шапке появляется `⎇ ветка` (+`•` при изменениях) и `↑/↓` токены, обновляются ~каждые 5с; правка файла → появляется `•`. Сайдбар показывает сумму. Закрытие всех плиток останавливает поллинг.

- [ ] **Step 6: Commit**

```bash
git add src/sessions.ts src/styles.css
git commit -m "feat: git + token badges on tiles, sidebar token summary, 5s polling"
```

---

## Task 8: Спайк — прозрачное always-on-top второе окно (decision gate)

Цель: до реализации пилюли подтвердить, что transparent + always-on-top второе окно рисуется поверх других приложений на macOS.

**Files:** временные правки `src-tauri/tauri.conf.json` (+возможно `Cargo.toml` feature) — по итогу зафиксировать рабочую конфигурацию в этом разделе.

- [ ] **Step 1: Пробное окно**

Создать `pill.html` (заглушка: `<body style="background:transparent"><div style="background:#222;color:#fff;padding:8px;border-radius:999px">тест</div></body>`) и во `vite.config.ts` добавить multi-page input (см. Task 9 Step 1). В `main.rs` `.setup(...)` временно создать окно:
```rust
let _ = tauri::WebviewWindowBuilder::new(app, "pill", tauri::WebviewUrl::App("pill.html".into()))
    .inner_size(180.0, 44.0).decorations(false).always_on_top(true)
    .transparent(true).skip_taskbar(true).build();
```
Если `transparent(true)` требует — добавить `"app": { "macOSPrivateApi": true }` в `tauri.conf.json` и/или Cargo feature `tauri = { features = ["macos-private-api"] }`.

- [ ] **Step 2: Запуск и наблюдение**

Run: `npm run tauri dev`
Наблюдать: окно-пилюля видно поверх других приложений; фон прозрачный (виден только «пузырёк»).

- [ ] **Step 3: Зафиксировать результат**

- Успех → записать рабочую конфигурацию (нужен ли `macOSPrivateApi`, feature) сюда и использовать в Task 9. Отметь: `- [x] transparent-pill: OK (config: …)`.
- Прозрачность капризит → fallback: непрозрачное окно (без `.transparent(true)`, тёмный фон CSS). Отметь: `- [x] transparent-pill: OPAQUE fallback`.
- Убрать временное создание окна из `main.rs` (реальное — в Task 9), но `pill.html`-заглушку и vite-input можно оставить (Task 9 их наполнит).

- [ ] **Step 4: Commit (конфиг + заглушки)**

```bash
git add src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock vite.config.ts pill.html
git commit -m "chore: spike transparent always-on-top pill window; record working config" --allow-empty
```

---

## Task 9: Пилюля «N ждут ввода» — второе окно + событийная связь

**Files:**
- Create: `pill.html`, `src/pill.ts`
- Modify: `vite.config.ts` (multi-page input)
- Modify: `src-tauri/src/main.rs` (создать окно пилюли скрытым при старте)
- Modify: `src-tauri/capabilities/default.json` (метка окна `pill` + нужные разрешения) или `capabilities/pill.json`
- Modify: `src/sessions.ts` (эмит `pill://count` при изменении waiting-count)
- Modify: `src/main.ts` (слушать `pill://focus-next` → raise + focusNextWaiting)
- Create: `tests/pill.test.ts`

**Interfaces:**
- Consumes: `waitingCount` (`src/sessions.ts`), `focusNextWaiting` (`Deck`), raise-логика окна (по образцу `src/notify.ts`)
- Produces: событие `pill://count` (payload `{ n: number }`), `pill://focus-next`; `pillLabel(n: number): string`

- [ ] **Step 1: vite multi-page + `pill.html`**

`vite.config.ts` — добавить в `build`:
```ts
  build: {
    target: "es2021", outDir: "dist",
    rollupOptions: { input: { main: "index.html", pill: "pill.html" } },
  },
```
`pill.html` (в корне):
```html
<!doctype html>
<html lang="ru"><head><meta charset="UTF-8" /><link rel="stylesheet" href="/src/pill.css" /></head>
<body><div id="pill" data-tauri-drag-region><span id="pill-text"></span></div>
<script type="module" src="/src/pill.ts"></script></body></html>
```
(создать `src/pill.css`: прозрачный body, тёмный «пузырёк» `#pill` с `border-radius:999px`, курсор pointer. Если спайк дал OPAQUE fallback — тёмный фон и у body.)

- [ ] **Step 2: Тест `pillLabel` (падает)**

`tests/pill.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { pillLabel } from "../src/pill";

describe("pillLabel", () => {
  it("singular for 1", () => expect(pillLabel(1)).toBe("1 ждёт ввода"));
  it("plural for 2", () => expect(pillLabel(2)).toBe("2 ждут ввода"));
  it("plural for 5", () => expect(pillLabel(5)).toBe("5 ждут ввода"));
  it("singular for 21", () => expect(pillLabel(21)).toBe("21 ждёт ввода"));
});
```

- [ ] **Step 3: Запуск — падает**

Run: `npm test -- pill`
Expected: FAIL.

- [ ] **Step 4: `src/pill.ts`**

```ts
import { listen, emit } from "@tauri-apps/api/event";
import { getCurrentWindow } from "@tauri-apps/api/window";

export function pillLabel(n: number): string {
  const verb = n % 10 === 1 && n % 100 !== 11 ? "ждёт" : "ждут";
  return `${n} ${verb} ввода`;
}

const win = getCurrentWindow();
const text = document.getElementById("pill-text")!;
const pill = document.getElementById("pill")!;

void listen<{ n: number }>("pill://count", async (e) => {
  const n = e.payload.n;
  if (n > 0) { text.textContent = pillLabel(n); await win.show(); }
  else { await win.hide(); }
});
pill.addEventListener("click", () => { void emit("pill://focus-next"); });
```
(Импорт `emit`/`listen` доступны из `@tauri-apps/api/event`; тест `pillLabel` не выполняет модульный код с window — обернуть побочные эффекты так, чтобы импорт в vitest не падал: если `document.getElementById` вернёт null в node-среде, тест сломается → пометить файл `tests/pill.test.ts` как `// @vitest-environment jsdom` и убедиться, что модульный код терпим к отсутствию элементов, либо вынести `pillLabel` в отдельный модуль `src/pill-util.ts` и импортировать его в тест. **Выбор:** вынести `pillLabel` в `src/pill-util.ts`, а `src/pill.ts` импортирует и использует его; тест импортирует из `../src/pill-util`.)

Уточнение к Step 2/4: создать `src/pill-util.ts` с `pillLabel`, тест импортирует оттуда; `src/pill.ts` — только рантайм окна, импортирует `pillLabel` из `./pill-util`.

- [ ] **Step 5: Создать окно пилюли в `main.rs`**

В `.setup(...)` (после manage AppState) добавить создание скрытого окна:
```rust
let _ = tauri::WebviewWindowBuilder::new(app, "pill", tauri::WebviewUrl::App("pill.html".into()))
    .inner_size(200.0, 48.0)
    .position(40.0, 40.0)
    .decorations(false)
    .always_on_top(true)
    .skip_taskbar(true)
    .visible(false)
    /* .transparent(true) — если спайк подтвердил */
    .build()?;
```
(флаг `.transparent(true)` и `macOSPrivateApi` — согласно результату Task 8.)

- [ ] **Step 6: Capability для окна пилюли**

В `capabilities/default.json` расширить `"windows": ["main", "pill"]` и убедиться, что разрешения событий (`core:event:default`) и окна (`core:window:allow-show`, `allow-hide`, `allow-set-focus`) покрывают обе метки. При необходимости — отдельный файл `capabilities/pill.json` c `"windows": ["pill"]`. Сверить строки в `src-tauri/gen/schemas/`.

- [ ] **Step 7: Эмит счётчика из `sessions.ts`**

Добавить импорт `import { emit } from "@tauri-apps/api/event";` в `src/sessions.ts`. В `renderList` (где уже считается `waiting`) добавить: `void emit("pill://count", { n: waiting });` (renderList вызывается при каждом изменении состояния/набора — это и есть событийный источник).

- [ ] **Step 8: Слушатель focus-next в `main.ts`**

Добавить в `src/main.ts`:
```ts
import { listen } from "@tauri-apps/api/event";
import { getCurrentWindow } from "@tauri-apps/api/window";
// …
void listen("pill://focus-next", async () => {
  const w = getCurrentWindow();
  await w.unminimize().catch(() => {});
  await w.show().catch(() => {});
  await w.setFocus().catch(() => {});
  deck.focusNextWaiting();
});
```
(Если в `notify.ts` уже есть готовая функция подъёма окна — переиспользовать её вместо дублирования.)

- [ ] **Step 9: Тесты и типы**

Run: `npm test -- pill` → PASS; `npm test` → PASS; `npx tsc --noEmit` → чисто

- [ ] **Step 10: Ручная проверка (десктоп, чеклист)**

Довести сессию до «ждёт ввода» → пилюля появляется поверх других приложений с «1 ждёт ввода»; несколько → «N ждут ввода»; клик → главное окно поднимается и фокусирует следующую ждущую; когда никто не ждёт → пилюля скрыта.

- [ ] **Step 11: Commit**

```bash
git add pill.html src/pill.ts src/pill-util.ts src/pill.css vite.config.ts src-tauri/src/main.rs src-tauri/capabilities/ src/sessions.ts src/main.ts tests/pill.test.ts
git commit -m "feat: floating status pill (N waiting) as transparent always-on-top window"
```

---

## Task 10: Финальное whole-branch ревью + документация

**Files:**
- Modify: `docs/superpowers/plans/RELEASE-1.0-CHECKLIST.md` (раздел «Группа C» с ручными проверками задач 3/7/9 + результат спайка пилюли)

- [ ] **Step 1: Полный прогон**

Run: `npm test` → зелёные; `cargo test --manifest-path src-tauri/Cargo.toml` → зелёные; `npx tsc --noEmit` → чисто

- [ ] **Step 2: Whole-branch ревью**

`superpowers:requesting-code-review` (opus) по всему диффу `feat/group-c` vs `main`. Особое внимание: (а) поллинг не создаёт нагрузку/утечек (интервал очищается при 0 плиток; нет бесконечных анимаций — ADR-008); (б) `session_tokens` толерантен к мусору/отсутствию файла; (в) git-дедуп по cwd; (г) capability и метки окон корректны; (д) кросс-оконные события pill не текут и не дублируются; (е) `saveUiState` best-effort (не роняет UI).

- [ ] **Step 3: Чеклист ручной приёмки**

Добавить в `RELEASE-1.0-CHECKLIST.md` раздел «Группа C»: персист окна/пространства, git-бейдж, токены+сводка, пилюля; строку результата спайка пилюли (OK/OPAQUE).

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/plans/RELEASE-1.0-CHECKLIST.md
git commit -m "docs: Group C manual acceptance checklist + pill spike result"
```

- [ ] **Step 5: Готовность к PR**

Ветка `feat/group-c` готова к PR в `main` (`superpowers:finishing-a-development-branch`), затем живая приёмка на десктопе.

---

## Self-Review (автор плана)

**Spec coverage:**
- Фича 1 (персист окна/пространства) → Task 1 (window-state) + Task 2 (ui_state Rust) + Task 3 (TS restore). ✓
- Фича 2 (git-индикатор) → Task 4 (Rust) + Task 6 (ipc/дедуп) + Task 7 (бейдж/поллинг). ✓
- Фича 3 (токены) → Task 5 (Rust) + Task 6 (ipc/формат/сумма) + Task 7 (бейдж/сводка/поллинг). ✓
- Фича 4 (пилюля) → Task 8 (спайк) + Task 9 (окно + события). ✓
- Cross-cutting (поллинг 5с, capability, уроки vitest) → Task 7, Task 9, Task 10. ✓

**Type consistency:** `GitStatus`/`TokenUsage`/`UiState` совпадают Rust↔TS (serde rename camelCase ↔ TS-интерфейс); `session_tokens(session_id)` ↔ ipc `sessionTokens(sessionId)` (Tauri camelCase-маппинг); `pillLabel` вынесен в `pill-util.ts` для тестируемости без DOM.

**Placeholder scan:** код приведён во всех шагах. Единственная неопределённость — прозрачность окна пилюли — вынесена в спайк Task 8 с fallback (OPAQUE), результат прокидывается в Task 9 Step 5. ✓

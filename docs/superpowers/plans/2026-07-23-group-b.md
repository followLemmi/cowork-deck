# Group B — «Продуктивность агентов» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Рестарт/восстановление сессии сохраняет контекст claude, сценарии параметризуются плейсхолдерами, вчерашние плитки восстанавливаются при старте, а один ввод рассылается в несколько сессий (broadcast).

**Architecture:** Каждая плитка (`tile.session`, UUID) становится claude session id: первый запуск `--session-id`, рестарт/restore `--resume`. Плейсхолдеры и broadcast — чисто фронтенд (чистые функции + модалка + фан-аут по `writeSession`). Раскладка плиток персистится в `sessions.json` через новые Tauri-команды и восстанавливается на старте.

**Tech Stack:** Tauri v2, Rust (ядро), ванильный TypeScript + xterm.js, vitest (+ jsdom), cargo test. Новых зависимостей нет.

## Global Constraints

- Tauri v2, ядро Rust + ванильный TypeScript + xterm.js. Только ванильный TS, без фреймворков.
- Целевой расход памяти < 100 МБ; только лёгкие зависимости. **Группа B не добавляет новых зависимостей.**
- Тёмная тема, палитра One Dark, дизайн-токены из `styles.css` (`:root`). Анимации только на transform/opacity/box-shadow.
- Строки интерфейса — на русском, в существующем стиле.
- Команды тестов неизменны: `npm test` (vitest run), `cargo test --manifest-path src-tauri/Cargo.toml`. На старте ветки: 47 vitest / 15 cargo зелёные.
- Коммиты — Conventional Commits.
- Ветка `feat/group-b` (от `main`, merge `9a6d78b`). Спек: `docs/superpowers/specs/2026-07-23-group-b-design.md`.

**Deviation note (vs спек):** `SessionEntry` реализуется как `{ sessionId, cwd, name }` — поле `icon` из спека свёрнуто в `name` (храним готовую строку заголовка целиком). Это упрощение (YAGNI): restore рендерит `name` как есть.

---

## Task 1: Спайк — `--settings` + `--resume` в связке (decision gate)

Цель: до кодирования фич 1 и 3 убедиться, что хуки (`--settings <json>`) подхватываются при `--resume`. Если нет — восстановленные плитки перестанут репортить состояние; фиксируем fallback.

**Files:** none (исследование; результат вписать в этот раздел галочками).

- [ ] **Step 1: Собрать settings.json с хук-пробой**

```bash
D=$(mktemp -d); cd "$D"
MARK="$D/hook-fired.txt"
cat > settings.json <<JSON
{ "hooks": { "UserPromptSubmit": [ { "hooks": [ { "type": "command", "command": "echo fired >> $MARK" } ] } ] } }
JSON
SID=$(uuidgen | tr 'A-Z' 'a-z'); echo "SID=$SID MARK=$MARK"
```

- [ ] **Step 2: Первый запуск с `--session-id` + `--settings`**

Run:
```bash
claude --session-id "$SID" --settings "$(cat settings.json)" -p "say A" 2>&1 | tail -2
echo "after-first: $(wc -l < "$MARK" 2>/dev/null || echo 0) lines"
```
Expected: файл `$MARK` появился (хук сработал на первом запуске).

- [ ] **Step 3: Resume с `--settings`**

Run:
```bash
claude --resume "$SID" --settings "$(cat settings.json)" -p "say B" 2>&1 | tail -2
echo "after-resume: $(wc -l < "$MARK" 2>/dev/null || echo 0) lines"
```
Expected (успех спайка): счётчик строк в `$MARK` **вырос** → хук из `--settings` сработал и на resume.

- [ ] **Step 4: Зафиксировать решение**

- Если счётчик вырос → **успех**: реализуем фичи 1/3 как задумано (передаём `--settings` и на resume). Отметь галочкой: `- [x] hooks-on-resume: OK`.
- Если НЕ вырос → **fallback**: в задачах 2/10 всё равно передаём `--settings` на resume (не хуже текущего), но пометь известную деградацию «метки состояния не обновляются у восстановленных сессий» в `docs/superpowers/plans/RELEASE-1.0-CHECKLIST.md` и в wrapup; вынеси на решение владельца. Отметь: `- [x] hooks-on-resume: DEGRADED (документировано)`.

- [ ] **Step 5: Commit (только правки плана/чеклиста, если были)**

```bash
git add docs/superpowers/plans/
git commit -m "docs: spike result for --settings + --resume (Group B task 1)" --allow-empty
```

---

## Task 2: Rust — `build_claude_args` c session-id/resume + `start_session`

**Files:**
- Modify: `src-tauri/src/commands.rs` (`build_claude_args` :16-24, `start_session` :82-114, tests-модуль :134-151)

**Interfaces:**
- Produces: `build_claude_args(settings_json: &str, initial_prompt: &Option<String>, session_id: &str, resume: bool) -> Vec<String>`
- Produces: `start_session(app, state, session: String, cwd: String, initial_prompt: Option<String>, cols: u16, rows: u16, resume: bool) -> Result<(), String>` (Tauri-команда; фронт передаёт `resume`)

- [ ] **Step 1: Обновить существующие тесты и добавить новые (сначала падают)**

Заменить тела двух существующих тестов в `mod tests` и добавить третий:

```rust
    #[test]
    fn builds_claude_args_first_launch_with_session_id_and_prompt() {
        let args = build_claude_args("{\"hooks\":{}}", &Some("collect email report".into()), "sess-1", false);
        assert_eq!(args, vec![
            "--settings".to_string(), "{\"hooks\":{}}".to_string(),
            "--session-id".to_string(), "sess-1".to_string(),
            "collect email report".to_string(),
        ]);
    }

    #[test]
    fn builds_claude_args_first_launch_without_prompt() {
        let args = build_claude_args("{}", &None, "sess-1", false);
        assert_eq!(args, vec![
            "--settings".to_string(), "{}".to_string(),
            "--session-id".to_string(), "sess-1".to_string(),
        ]);
    }

    #[test]
    fn builds_claude_args_resume_uses_resume_flag_and_ignores_prompt() {
        let args = build_claude_args("{}", &Some("ignored".into()), "sess-1", true);
        assert_eq!(args, vec![
            "--settings".to_string(), "{}".to_string(),
            "--resume".to_string(), "sess-1".to_string(),
        ]);
    }
```

- [ ] **Step 2: Запустить — падает (сигнатура ещё старая)**

Run: `cargo test --manifest-path src-tauri/Cargo.toml build_claude_args`
Expected: FAIL (несоответствие числа аргументов у `build_claude_args`).

- [ ] **Step 3: Реализовать новую `build_claude_args`**

```rust
/// Build the argv (after the program name) for launching an interactive claude
/// session. First launch pins our own session id via `--session-id`; restart/
/// restore resumes that same conversation via `--resume` (no prompt — context
/// already lives in the resumed session).
pub fn build_claude_args(
    settings_json: &str,
    initial_prompt: &Option<String>,
    session_id: &str,
    resume: bool,
) -> Vec<String> {
    let mut args = vec!["--settings".to_string(), settings_json.to_string()];
    if resume {
        args.push("--resume".to_string());
        args.push(session_id.to_string());
    } else {
        args.push("--session-id".to_string());
        args.push(session_id.to_string());
        if let Some(p) = initial_prompt {
            args.push(p.clone());
        }
    }
    args
}
```

- [ ] **Step 4: Пробросить `resume` в `start_session`**

В сигнатуру `start_session` добавить `resume: bool` (после `rows: u16,`) и заменить строку сборки аргументов:

```rust
    let args = build_claude_args(&settings, &initial_prompt, &session, resume);
```

- [ ] **Step 5: Запустить тесты — зелёные**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS (16 тестов: 15 прежних + 1 новый; два старых теста заменены).

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/commands.rs
git commit -m "feat: build_claude_args pins --session-id and resumes via --resume"
```

---

## Task 3: TS — IPC/terminal/⟳ проброс `resume`

**Files:**
- Modify: `src/ipc.ts:16-18` (`startSession`)
- Modify: `src/terminal.ts:57-60` (`start`)
- Modify: `src/sessions.ts:56-71` (restart onclick), `:100-101` (первый launch остаётся `resume=false`)
- Modify: `tests/ipc.test.ts:21-27` (тест `startSession`)

**Interfaces:**
- Consumes: Tauri-команда `start_session` с параметром `resume` (Task 2)
- Produces: `startSession(session, cwd, initialPrompt, cols, rows, resume: boolean)`; `TerminalPanel.start(cwd, initialPrompt, resume?: boolean)`

- [ ] **Step 1: Обновить тест `startSession` (падает)**

Заменить тело теста «startSession passes all params» в `tests/ipc.test.ts`:

```ts
  it("startSession passes all params incl. resume", async () => {
    vi.mocked(invoke).mockResolvedValue(undefined);
    await startSession("s1", "/proj", "do the thing", 80, 24, false);
    expect(invoke).toHaveBeenCalledWith("start_session", {
      session: "s1", cwd: "/proj", initialPrompt: "do the thing", cols: 80, rows: 24, resume: false,
    });
  });
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test -- ipc`
Expected: FAIL (в вызове нет `resume`).

- [ ] **Step 3: Реализовать проброс в `ipc.ts`**

```ts
export const startSession = (
  session: string, cwd: string, initialPrompt: string | null, cols: number, rows: number, resume: boolean,
) => invoke<void>("start_session", { session, cwd, initialPrompt, cols, rows, resume });
```

- [ ] **Step 4: `TerminalPanel.start` принимает `resume`**

В `src/terminal.ts`:

```ts
  async start(cwd: string, initialPrompt: string | null, resume = false) {
    const { cols, rows } = this.term;
    await startSession(this.session, cwd, initialPrompt, cols, rows, resume);
  }
```

- [ ] **Step 5: Кнопка ⟳ возобновляет (resume=true, без промпта)**

В `src/sessions.ts` в обработчике `restart.onclick` заменить строку старта:

```ts
        await tile.panel.start(tile.workspacePath, null, true);
```
(первый запуск на строке ~101 `await panel.start(workspace.path, skill ? skill.prompt : null);` не трогаем — `resume` по умолчанию `false`.)

- [ ] **Step 6: Тесты и типы зелёные**

Run: `npm test -- ipc` → PASS
Run: `npx tsc --noEmit` → без ошибок

- [ ] **Step 7: Commit**

```bash
git add src/ipc.ts src/terminal.ts src/sessions.ts tests/ipc.test.ts
git commit -m "feat: restart resumes the claude conversation (--resume) via panel.start(resume)"
```

---

## Task 4: TS — модуль плейсхолдеров (`src/placeholders.ts`)

**Files:**
- Create: `src/placeholders.ts`
- Create: `tests/placeholders.test.ts`

**Interfaces:**
- Produces: `parsePlaceholders(prompt: string): string[]`
- Produces: `fillPlaceholders(prompt: string, values: Record<string, string>): string`
- Produces: `resolvePrompt(prompt: string, ask: (names: string[]) => Promise<Record<string, string> | null>): Promise<string | null>`

- [ ] **Step 1: Тесты (падают)**

`tests/placeholders.test.ts`:

```ts
import { describe, it, expect, vi } from "vitest";
import { parsePlaceholders, fillPlaceholders, resolvePrompt } from "../src/placeholders";

describe("parsePlaceholders", () => {
  it("returns [] when none", () => {
    expect(parsePlaceholders("just a prompt")).toEqual([]);
  });
  it("extracts unique names in order of appearance", () => {
    expect(parsePlaceholders("fix {{ticket}} on {{branch}}, retest {{ticket}}"))
      .toEqual(["ticket", "branch"]);
  });
  it("tolerates inner whitespace", () => {
    expect(parsePlaceholders("{{  branch  }}")).toEqual(["branch"]);
  });
});

describe("fillPlaceholders", () => {
  it("replaces all occurrences", () => {
    expect(fillPlaceholders("{{a}}-{{a}}-{{b}}", { a: "1", b: "2" })).toBe("1-1-2");
  });
  it("leaves unknown placeholders untouched", () => {
    expect(fillPlaceholders("{{a}}/{{b}}", { a: "x" })).toBe("x/{{b}}");
  });
});

describe("resolvePrompt", () => {
  it("returns prompt unchanged and does not ask when no placeholders", async () => {
    const ask = vi.fn();
    expect(await resolvePrompt("plain", ask)).toBe("plain");
    expect(ask).not.toHaveBeenCalled();
  });
  it("asks and fills when placeholders present", async () => {
    const ask = vi.fn().mockResolvedValue({ branch: "feat/x" });
    expect(await resolvePrompt("on {{branch}}", ask)).toBe("on feat/x");
    expect(ask).toHaveBeenCalledWith(["branch"]);
  });
  it("returns null when the user cancels", async () => {
    const ask = vi.fn().mockResolvedValue(null);
    expect(await resolvePrompt("on {{branch}}", ask)).toBeNull();
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test -- placeholders`
Expected: FAIL (модуль не существует).

- [ ] **Step 3: Реализовать `src/placeholders.ts`**

```ts
// Scenario prompts may contain {{name}} placeholders. At launch we collect a
// value per unique placeholder and substitute before sending the first message.
const RE = /\{\{\s*([\w-]+)\s*\}\}/g;

export function parsePlaceholders(prompt: string): string[] {
  const seen = new Set<string>();
  const out: string[] = [];
  for (const m of prompt.matchAll(RE)) {
    const name = m[1];
    if (!seen.has(name)) { seen.add(name); out.push(name); }
  }
  return out;
}

export function fillPlaceholders(prompt: string, values: Record<string, string>): string {
  return prompt.replace(RE, (whole, name: string) =>
    Object.prototype.hasOwnProperty.call(values, name) ? values[name] : whole);
}

/** Resolve a prompt's placeholders. No placeholders → returns the prompt as-is
 *  without asking. Otherwise calls `ask` with the names; returns the filled
 *  prompt, or null if the user cancelled. */
export async function resolvePrompt(
  prompt: string,
  ask: (names: string[]) => Promise<Record<string, string> | null>,
): Promise<string | null> {
  const names = parsePlaceholders(prompt);
  if (names.length === 0) return prompt;
  const values = await ask(names);
  if (!values) return null;
  return fillPlaceholders(prompt, values);
}
```

- [ ] **Step 4: Тесты и типы зелёные**

Run: `npm test -- placeholders` → PASS
Run: `npx tsc --noEmit` → без ошибок

- [ ] **Step 5: Commit**

```bash
git add src/placeholders.ts tests/placeholders.test.ts
git commit -m "feat: placeholder parse/fill/resolve for scenario prompts"
```

---

## Task 5: TS — `placeholderForm` (модалка ввода значений)

**Files:**
- Modify: `src/forms.ts` (добавить экспорт `placeholderForm`, переиспользуя приватные `overlay`/`labeled`/`actions`)
- Modify: `tests/forms.test.ts` (добавить describe-блок)

**Interfaces:**
- Consumes: приватные `overlay()`, `labeled()`, `actions()` из `src/forms.ts`
- Produces: `placeholderForm(names: string[]): Promise<Record<string, string> | null>`

- [ ] **Step 1: Тесты (падают)**

Добавить в конец `tests/forms.test.ts` (файл уже `// @vitest-environment jsdom`):

```ts
import { placeholderForm } from "../src/forms";

describe("placeholderForm", () => {
  it("collects one value per name and resolves on OK", async () => {
    const p = placeholderForm(["branch", "ticket"]);
    const inputs = document.querySelectorAll<HTMLInputElement>(".form-ph");
    inputs[0].value = "feat/x";
    inputs[1].value = "JIRA-1";
    document.querySelector<HTMLButtonElement>(".modal-ok")!.click();
    expect(await p).toEqual({ branch: "feat/x", ticket: "JIRA-1" });
  });

  it("resolves null on cancel", async () => {
    const p = placeholderForm(["branch"]);
    document.querySelector<HTMLButtonElement>(".modal-cancel")!.click();
    expect(await p).toBeNull();
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test -- forms`
Expected: FAIL (`placeholderForm` не экспортирован).

- [ ] **Step 3: Реализовать `placeholderForm` в `src/forms.ts`**

Добавить в конец файла:

```ts
/** Prompt for one value per placeholder name (order preserved). Resolves a
 *  name→value map on OK, or null on Cancel/backdrop click. */
export function placeholderForm(names: string[]): Promise<Record<string, string> | null> {
  return new Promise((resolve) => {
    const { overlay: ov, box } = overlay();
    const title = document.createElement("div");
    title.className = "modal-title";
    title.textContent = "Параметры сценария";

    const inputs = new Map<string, HTMLInputElement>();
    const rows: HTMLElement[] = [];
    for (const n of names) {
      const inp = document.createElement("input");
      inp.className = "modal-input form-ph"; inp.type = "text";
      inp.dataset.name = n;
      inputs.set(n, inp);
      rows.push(labeled(n, inp));
    }

    const { row, ok, cancel } = actions();
    box.append(title, ...rows, row);

    const close = (v: Record<string, string> | null) => { ov.remove(); resolve(v); };
    ok.onclick = () => {
      const values: Record<string, string> = {};
      for (const [n, inp] of inputs) values[n] = inp.value.trim();
      close(values);
    };
    cancel.onclick = () => close(null);
    ov.addEventListener("mousedown", (e) => { if (e.target === ov) close(null); });
    inputs.get(names[0])?.focus();
  });
}
```

- [ ] **Step 4: Тесты и типы зелёные**

Run: `npm test -- forms` → PASS
Run: `npx tsc --noEmit` → без ошибок

- [ ] **Step 5: Commit**

```bash
git add src/forms.ts tests/forms.test.ts
git commit -m "feat: placeholderForm modal for scenario parameter input"
```

---

## Task 6: TS — подстановка плейсхолдеров при запуске сценария

**Files:**
- Modify: `src/main.ts:30-33` (`SkillsPanel` onLaunch callback)

**Interfaces:**
- Consumes: `resolvePrompt` (Task 4), `placeholderForm` (Task 5), `Deck.launch(workspace, skill)` (существует)

- [ ] **Step 1: Реализовать проброс через `resolvePrompt`**

В `src/main.ts` добавить импорты:

```ts
import { resolvePrompt } from "./placeholders";
import { placeholderForm } from "./forms";
```

Заменить конструктор `SkillsPanel`:

```ts
const skills = new SkillsPanel(skMount, () => workspaces.active?.id ?? null, async (skill) => {
  const ws = workspaces.active;
  if (!ws) return;
  const prompt = await resolvePrompt(skill.prompt, placeholderForm);
  if (prompt === null) return; // отмена в форме параметров
  deck.launch(ws, { ...skill, prompt });
});
```

- [ ] **Step 2: Проверка типов**

Run: `npx tsc --noEmit`
Expected: без ошибок (`onLaunch` допускает `async`, т.к. возвращаемое значение не используется — сигнатура `(skill: Skill) => void`).

- [ ] **Step 3: Полный прогон тестов**

Run: `npm test`
Expected: PASS (регрессий нет).

- [ ] **Step 4: Ручная проверка (десктоп, отметить в чеклисте)**

Сценарий с `{{branch}}` в задании → при запуске появляется модалка «Параметры сценария»; после ввода claude получает подставленный текст. Сценарий без `{{…}}` → запускается без модалки.

- [ ] **Step 5: Commit**

```bash
git add src/main.ts
git commit -m "feat: prompt for {{placeholder}} values before launching a scenario"
```

---

## Task 7: Rust — `SessionEntry` + персист раскладки в `store.rs`

**Files:**
- Modify: `src-tauri/src/model.rs` (добавить `SessionEntry` после `Skill` :28)
- Modify: `src-tauri/src/store.rs:1` (импорт), `impl Store` (методы `layout`/`save_layout`), tests-модуль

**Interfaces:**
- Produces: `model::SessionEntry { session_id: String (serde "sessionId"), cwd: String, name: String }`
- Produces: `Store::layout(&self) -> Vec<SessionEntry>`, `Store::save_layout(&self, &[SessionEntry]) -> std::io::Result<()>`

- [ ] **Step 1: Тест round-trip (падает)**

Добавить в `mod tests` в `src-tauri/src/store.rs` (импорт `use crate::model::SessionEntry;` внутри `mod tests` рядом с `use crate::model::Workspace;`):

```rust
    #[test]
    fn layout_round_trips_and_defaults_empty() {
        let s = Store::new(tmp());
        assert!(s.layout().is_empty()); // NotFound -> empty
        let entries = vec![
            SessionEntry { session_id: "s1".into(), cwd: "/tmp/a".into(), name: "▶ Fix".into() },
            SessionEntry { session_id: "s2".into(), cwd: "/tmp/b".into(), name: "терминал · P".into() },
        ];
        s.save_layout(&entries).unwrap();
        let reloaded = Store::new(s.dir.clone()).layout();
        assert_eq!(reloaded, entries);
    }
```

- [ ] **Step 2: Запустить — падает**

Run: `cargo test --manifest-path src-tauri/Cargo.toml layout_round_trips`
Expected: FAIL (`SessionEntry`/методы не существуют).

- [ ] **Step 3: Добавить `SessionEntry` в `model.rs`**

После структуры `Skill`:

```rust
/// A persisted tile in the deck layout — enough to reopen it and resume its
/// claude conversation on next launch. The PTY itself is not persisted.
#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
pub struct SessionEntry {
    #[serde(rename = "sessionId")]
    pub session_id: String,
    pub cwd: String,
    pub name: String,
}
```

- [ ] **Step 4: Методы в `store.rs`**

Изменить импорт в начале файла (`src-tauri/src/store.rs:1`):

```rust
use crate::model::{SessionEntry, Skill, Workspace};
```

Добавить в `impl Store` рядом с `sk_path`/`skills`:

```rust
    fn layout_path(&self) -> PathBuf { self.dir.join("sessions.json") }
    pub fn layout(&self) -> Vec<SessionEntry> { Self::read_vec(&self.layout_path()) }
    pub fn save_layout(&self, items: &[SessionEntry]) -> std::io::Result<()> {
        Self::write_vec(&self.layout_path(), items)
    }
```

- [ ] **Step 5: Тесты зелёные**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src-tauri/src/model.rs src-tauri/src/store.rs
git commit -m "feat: persist deck layout (SessionEntry) in sessions.json"
```

---

## Task 8: Rust — команды `load_layout`/`save_layout` + регистрация

**Files:**
- Modify: `src-tauri/src/commands.rs` (импорт `SessionEntry`; две новые команды после `close_session`)
- Modify: `src-tauri/src/main.rs:82-94` (`generate_handler!`)

**Interfaces:**
- Consumes: `Store::layout`/`save_layout` (Task 7)
- Produces: Tauri-команды `load_layout() -> Vec<SessionEntry>`, `save_layout(sessions: Vec<SessionEntry>) -> Result<(), String>`

- [ ] **Step 1: Добавить команды в `commands.rs`**

В импорт модели добавить `SessionEntry` (там, где импортируются `Workspace`/`Skill`). После `close_session` добавить:

```rust
#[tauri::command]
pub fn load_layout(state: State<AppState>) -> Vec<SessionEntry> {
    state.store.lock().unwrap().layout()
}

#[tauri::command]
pub fn save_layout(state: State<AppState>, sessions: Vec<SessionEntry>) -> Result<(), String> {
    state.store.lock().unwrap().save_layout(&sessions).map_err(|e| e.to_string())
}
```

- [ ] **Step 2: Зарегистрировать в `main.rs`**

В `tauri::generate_handler![…]` добавить две строки после `commands::close_session,`:

```rust
            commands::load_layout,
            commands::save_layout,
```

- [ ] **Step 3: Сборка/тесты**

Run: `cargo test --manifest-path src-tauri/Cargo.toml`
Expected: PASS (компилируется, регистрация валидна).

Примечание: пользовательские `#[tauri::command]` не требуют записей в `capabilities/default.json` (это только для core/plugin-команд) — правок capability не нужно.

- [ ] **Step 4: Commit**

```bash
git add src-tauri/src/commands.rs src-tauri/src/main.rs
git commit -m "feat: load_layout/save_layout Tauri commands"
```

---

## Task 9: TS — IPC раскладки + чистый сериализатор плиток

**Files:**
- Modify: `src/ipc.ts` (тип `SessionEntry` + `loadLayout`/`saveLayout`)
- Create: `tests/layout.test.ts`
- Modify: `src/sessions.ts` (экспорт чистой функции `serializeTiles`)

**Interfaces:**
- Consumes: команды `load_layout`/`save_layout` (Task 8)
- Produces: `SessionEntry { sessionId: string; cwd: string; name: string }`; `loadLayout(): Promise<SessionEntry[]>`; `saveLayout(sessions: SessionEntry[]): Promise<void>`
- Produces: `serializeTiles(tiles: { session: string; workspacePath: string; name: string }[]): SessionEntry[]`

- [ ] **Step 1: Тесты (падают)**

`tests/layout.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

vi.mock("@tauri-apps/api/core");
vi.mock("@tauri-apps/api/event");

import { loadLayout, saveLayout } from "../src/ipc";
import { serializeTiles } from "../src/sessions";
import { invoke } from "@tauri-apps/api/core";

beforeEach(() => vi.clearAllMocks());

describe("layout ipc", () => {
  it("loadLayout calls load_layout", async () => {
    vi.mocked(invoke).mockResolvedValue([{ sessionId: "s1", cwd: "/a", name: "N" }]);
    const res = await loadLayout();
    expect(invoke).toHaveBeenCalledWith("load_layout");
    expect(res[0].sessionId).toBe("s1");
  });
  it("saveLayout passes sessions", async () => {
    vi.mocked(invoke).mockResolvedValue(undefined);
    await saveLayout([{ sessionId: "s1", cwd: "/a", name: "N" }]);
    expect(invoke).toHaveBeenCalledWith("save_layout", { sessions: [{ sessionId: "s1", cwd: "/a", name: "N" }] });
  });
});

describe("serializeTiles", () => {
  it("maps tile fields to SessionEntry shape", () => {
    expect(serializeTiles([{ session: "s1", workspacePath: "/a", name: "▶ Fix" }]))
      .toEqual([{ sessionId: "s1", cwd: "/a", name: "▶ Fix" }]);
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test -- layout`
Expected: FAIL (не существуют `loadLayout`/`saveLayout`/`serializeTiles`).

- [ ] **Step 3: IPC в `src/ipc.ts`**

Добавить тип рядом с `Skill` и функции рядом с остальными командами:

```ts
export interface SessionEntry { sessionId: string; cwd: string; name: string; }

export const loadLayout = () => invoke<SessionEntry[]>("load_layout");
export const saveLayout = (sessions: SessionEntry[]) => invoke<void>("save_layout", { sessions });
```

- [ ] **Step 4: `serializeTiles` в `src/sessions.ts`**

Добавить импорт типа в начале файла (`SessionEntry`) в существующий импорт из `./ipc`, и экспортировать функцию рядом с `waitingCount`:

```ts
export function serializeTiles(
  tiles: { session: string; workspacePath: string; name: string }[],
): SessionEntry[] {
  return tiles.map((t) => ({ sessionId: t.session, cwd: t.workspacePath, name: t.name }));
}
```

- [ ] **Step 5: Тесты и типы зелёные**

Run: `npm test -- layout` → PASS
Run: `npx tsc --noEmit` → без ошибок

- [ ] **Step 6: Commit**

```bash
git add src/ipc.ts src/sessions.ts tests/layout.test.ts
git commit -m "feat: layout IPC (load/save) and serializeTiles helper"
```

---

## Task 10: TS — персист раскладки + авто-restore при старте

**Files:**
- Modify: `src/sessions.ts` (рефактор `launch` → приватный `spawnTile`; `persistLayout`; публичный `restore`)
- Modify: `src/main.ts` (boot: `loadLayout` → `deck.restore`)

**Interfaces:**
- Consumes: `saveLayout`/`loadLayout`/`SessionEntry` (Task 9), `serializeTiles` (Task 9), `TerminalPanel.start(cwd, prompt, resume)` (Task 3)
- Produces: `Deck.restore(entries: SessionEntry[]): Promise<void>`

- [ ] **Step 1: Рефактор `launch` в `spawnTile` (без изменения поведения)**

В `src/sessions.ts` выделить построение плитки в приватный метод. Заменить метод `launch` целиком на пару `launch` + `spawnTile`:

```ts
  async launch(workspace: Workspace, skill: Skill | null) {
    const titleText = skill ? `${skill.icon} ${skill.name}` : `терминал · ${workspace.name}`;
    await this.spawnTile({
      session: crypto.randomUUID(),
      cwd: workspace.path,
      titleText,
      prompt: skill ? skill.prompt : null,
      resume: false,
    });
  }

  private async spawnTile(opts: {
    session: string; cwd: string; titleText: string; prompt: string | null; resume: boolean;
  }) {
    const { session, cwd, titleText, prompt, resume } = opts;
    const el = document.createElement("div");
    el.className = "tile";
    const head = document.createElement("div");
    head.className = "tile-head";
    const title = document.createElement("span");
    title.textContent = titleText;
    const label = document.createElement("span");
    label.className = "tile-state state-idle"; label.textContent = LABEL.idle;
    const clearBtn = document.createElement("button");
    clearBtn.textContent = "⌫"; clearBtn.className = "tile-close"; clearBtn.title = "очистить";
    clearBtn.onclick = () => tile.panel.clear();
    const close = document.createElement("button");
    close.textContent = "✕"; close.className = "tile-close";
    close.onclick = () => this.remove(session);
    head.append(title, label, clearBtn, close);
    const restart = document.createElement("button");
    restart.textContent = "⟳"; restart.className = "tile-close"; restart.style.display = "none";
    restart.title = "перезапустить";
    restart.onclick = async () => {
      restart.style.display = "none";
      tile.panel.write("\r\n[перезапуск сессии...]\r\n");
      try {
        await tile.panel.start(tile.workspacePath, null, true);
        this.setState(session, "idle");
      } catch (e) {
        this.setState(session, "error");
        const raw = String((e as { message?: string })?.message ?? e);
        const readable = raw.includes("claude-not-found")
          ? "claude не найден — укажите путь и перезапустите"
          : raw;
        tile.panel.write(`\r\n[ошибка запуска: ${readable}]\r\n`);
        restart.style.display = "inline";
      }
    };
    head.insertBefore(restart, close);
    const mount = document.createElement("div");
    mount.className = "tile-body";
    const searchBar = document.createElement("div");
    searchBar.className = "tile-search hidden";
    const sInput = document.createElement("input"); sInput.className = "tile-search-input"; sInput.placeholder = "поиск…";
    const sNext = document.createElement("button"); sNext.textContent = "▼"; sNext.className = "tile-search-btn";
    const sPrev = document.createElement("button"); sPrev.textContent = "▲"; sPrev.className = "tile-search-btn";
    const sClose = document.createElement("button"); sClose.textContent = "✕"; sClose.className = "tile-search-btn";
    searchBar.append(sInput, sPrev, sNext, sClose);
    sInput.addEventListener("keydown", (e) => {
      if (e.key === "Enter") { e.preventDefault(); tile.panel.search(sInput.value); }
      else if (e.key === "Escape") { e.preventDefault(); searchBar.classList.add("hidden"); tile.panel.focus(); }
    });
    sNext.onclick = () => tile.panel.search(sInput.value);
    sPrev.onclick = () => tile.panel.searchPrev(sInput.value);
    sClose.onclick = () => { searchBar.classList.add("hidden"); tile.panel.focus(); };
    el.append(head, searchBar, mount);
    this.deckEl.appendChild(el);
    el.addEventListener("mousedown", () => this.focusTile(session));

    const panel = new TerminalPanel(session, mount);
    const tile: Tile = {
      session, name: title.textContent!, panel, state: "idle", el, label,
      workspacePath: cwd, prompt, restartBtn: restart, searchBar,
    };
    this.tiles.set(session, tile);
    this.renderList();
    void this.persistLayout();
    try {
      await panel.start(cwd, prompt, resume);
    } catch (e) {
      this.setState(session, "error");
      const raw = String((e as { message?: string })?.message ?? e);
      const readable = raw.includes("claude-not-found")
        ? "claude не найден — укажите путь и перезапустите"
        : raw;
      panel.write(`\r\n[ошибка запуска: ${readable}]\r\n`);
    }
    this.focusTile(session);
  }
```

- [ ] **Step 2: `persistLayout` и `restore`**

Добавить импорты в начале файла: `saveLayout`, `loadLayout` не нужен здесь (loadLayout вызывает main.ts); достаточно `saveLayout` и `serializeTiles` (в этом же модуле). Добавить приватный `persistLayout` и публичный `restore`; вызвать `persistLayout` в `remove` (после `this.renderList()`):

```ts
  private persistLayout() {
    const entries = serializeTiles([...this.tiles.values()].map((t) => ({
      session: t.session, workspacePath: t.workspacePath, name: t.name,
    })));
    return saveLayout(entries).catch((e) => console.debug("saveLayout failed", e));
  }

  async restore(entries: SessionEntry[]) {
    for (const e of entries) {
      await this.spawnTile({
        session: e.sessionId, cwd: e.cwd, titleText: e.name, prompt: null, resume: true,
      });
    }
  }
```

В методе `remove` добавить `void this.persistLayout();` последней строкой перед `this.renderList();` → фактически после `this.tiles.delete(session)`; итог: удалить старый `this.renderList();` в конце `remove` и заменить на:

```ts
    this.renderList();
    void this.persistLayout();
```

- [ ] **Step 3: Boot-restore в `main.ts`**

Добавить импорт `loadLayout` в существующий импорт из `./ipc`. После `deck.wireNotificationFocus();` (и до/после загрузки панелей — порядок не критичен) добавить:

```ts
loadLayout().then((entries) => { if (entries.length) void deck.restore(entries); });
```

- [ ] **Step 4: Полный прогон + типы**

Run: `npm test` → PASS (регрессий нет; `serializeTiles`/layout уже покрыты Task 9)
Run: `npx tsc --noEmit` → без ошибок

- [ ] **Step 5: Ручная проверка (десктоп, чеклист)**

Открыть 2 сессии → закрыть приложение → открыть снова: обе плитки восстановлены, каждая возобновила свой разговор (`--resume`, виден прежний контекст). Удаление плитки обновляет `sessions.json`.

- [ ] **Step 6: Commit**

```bash
git add src/sessions.ts src/main.ts
git commit -m "feat: persist deck layout and auto-restore sessions on startup (--resume)"
```

---

## Task 11: TS — broadcast: чистый фан-аут + команда/хоткей/палитра

**Files:**
- Create: `src/broadcast.ts`
- Create: `tests/broadcast.test.ts`
- Modify: `src/commands.ts:9-20` (`matchHotkey`: `b` → `broadcast`)
- Modify: `tests/commands.test.ts` (кейс на `b`)
- Modify: `src/main.ts` (регистрация команды: `COMMANDS`, `paletteCommands`, диспетчеризация)

**Interfaces:**
- Consumes: `writeSession` (`src/ipc.ts`), `Deck.toggleBroadcast()` (Task 12 — регистрируется здесь, реализуется в Task 12)
- Produces: `broadcastInput(sessions: string[], text: string): void`; hotkey id `"broadcast"`

- [ ] **Step 1: Тесты фан-аута (падают)**

`tests/broadcast.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";

vi.mock("../src/ipc", () => ({ writeSession: vi.fn() }));

import { broadcastInput } from "../src/broadcast";
import { writeSession } from "../src/ipc";

beforeEach(() => vi.clearAllMocks());

describe("broadcastInput", () => {
  it("writes text + CR to each target session", () => {
    broadcastInput(["a", "b"], "hello");
    expect(writeSession).toHaveBeenCalledTimes(2);
    expect(writeSession).toHaveBeenCalledWith("a", "hello\r");
    expect(writeSession).toHaveBeenCalledWith("b", "hello\r");
  });
  it("is a no-op with no targets", () => {
    broadcastInput([], "x");
    expect(writeSession).not.toHaveBeenCalled();
  });
});
```

И добавить кейс в `tests/commands.test.ts` в describe `matchHotkey`:

```ts
  it("maps Cmd+B to broadcast on macOS", () => {
    expect(matchHotkey({ ...base, key: "b", metaKey: true }, true)).toBe("broadcast");
  });
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test -- broadcast commands`
Expected: FAIL (нет `broadcast.ts`; `matchHotkey` не знает `b`).

- [ ] **Step 3: Реализовать `src/broadcast.ts`**

```ts
import { writeSession } from "./ipc";

/** Send one line of input (with a trailing CR) to each target session. */
export function broadcastInput(sessions: string[], text: string): void {
  const data = text + "\r";
  for (const s of sessions) void writeSession(s, data);
}
```

- [ ] **Step 4: `matchHotkey` → `broadcast`**

В `src/commands.ts` в `matchHotkey` добавить перед строкой с `focus-`:

```ts
  if (k === "b" && !e.shiftKey) return "broadcast";
```

- [ ] **Step 5: Тесты и типы зелёные**

Run: `npm test -- broadcast commands` → PASS
Run: `npx tsc --noEmit` → без ошибок

(Регистрация команды `broadcast` в `main.ts` и метод `Deck.toggleBroadcast` — в Task 12, где `toggleBroadcast` реально реализуется. Здесь только чистый фан-аут и хоткей-маппинг — самодостаточно и без заглушек.)

- [ ] **Step 6: Commit**

```bash
git add src/broadcast.ts tests/broadcast.test.ts src/commands.ts tests/commands.test.ts
git commit -m "feat: broadcast fanout helper and Cmd/Ctrl+B hotkey mapping"
```

---

## Task 12: TS — broadcast UI (чекбоксы на плитках + панель ввода)

**Files:**
- Modify: `src/sessions.ts` (`Deck`: состояние режима, чекбоксы в `tile-head`, панель ввода, `toggleBroadcast`, отправка)
- Modify: `index.html` (контейнер панели broadcast в разметке deck) — при отсутствии подходящего узла создаём панель динамически в `Deck`
- Modify: `src/styles.css` (`.bcast-*` стили)
- Modify: `tests/sessions-broadcast.test.ts` (новый; юнит на set выбранных)

**Interfaces:**
- Consumes: `broadcastInput` (Task 11), `Tile` (существует)
- Produces: `Deck.toggleBroadcast()` (полная реализация вместо заглушки), внутренний `selectedSessions(): string[]`

- [ ] **Step 1: Тест выбора целей (падает)**

`tests/sessions-broadcast.test.ts`:

```ts
// @vitest-environment jsdom
import { describe, it, expect } from "vitest";
import { selectedFromChecks } from "../src/sessions";

describe("selectedFromChecks", () => {
  it("returns session ids of checked boxes", () => {
    const checks = [
      { session: "a", checked: true },
      { session: "b", checked: false },
      { session: "c", checked: true },
    ];
    expect(selectedFromChecks(checks)).toEqual(["a", "c"]);
  });
});
```

- [ ] **Step 2: Запустить — падает**

Run: `npm test -- sessions-broadcast`
Expected: FAIL (нет `selectedFromChecks`).

- [ ] **Step 3: Чистый помощник + состояние режима в `sessions.ts`**

Экспортировать помощник рядом с `serializeTiles`:

```ts
export function selectedFromChecks(checks: { session: string; checked: boolean }[]): string[] {
  return checks.filter((c) => c.checked).map((c) => c.session);
}
```

Добавить в `Tile` поле чекбокса: в интерфейс `Tile` добавить `bcastCheck: HTMLInputElement;`. В `spawnTile` при сборке `head` создать чекбокс (скрытый вне режима):

```ts
    const bcastCheck = document.createElement("input");
    bcastCheck.type = "checkbox"; bcastCheck.className = "bcast-check hidden";
    head.insertBefore(bcastCheck, title);
```
и добавить `bcastCheck` в литерал `tile: Tile = { … , bcastCheck }`.

Добавить поля и методы в класс `Deck`:

```ts
  private broadcasting = false;
  private bcastPanel: HTMLElement | null = null;

  toggleBroadcast() {
    this.broadcasting = !this.broadcasting;
    for (const t of this.tiles.values()) t.bcastCheck.classList.toggle("hidden", !this.broadcasting);
    if (this.broadcasting) this.showBroadcastPanel();
    else this.hideBroadcastPanel();
  }

  private showBroadcastPanel() {
    if (!this.bcastPanel) {
      const panel = document.createElement("div");
      panel.className = "bcast-panel";
      const input = document.createElement("input");
      input.className = "bcast-input"; input.type = "text";
      input.placeholder = "broadcast: ввод во все отмеченные сессии, Enter — отправить";
      input.addEventListener("keydown", (e) => {
        if (e.key === "Enter") {
          e.preventDefault();
          const targets = selectedFromChecks(
            [...this.tiles.values()].map((t) => ({ session: t.session, checked: t.bcastCheck.checked })));
          broadcastInput(targets, input.value);
          input.value = "";
        } else if (e.key === "Escape") {
          e.preventDefault(); this.toggleBroadcast();
        }
      });
      panel.append(input);
      this.deckEl.appendChild(panel);
      this.bcastPanel = panel;
    }
    this.bcastPanel.classList.remove("hidden");
    (this.bcastPanel.querySelector(".bcast-input") as HTMLInputElement).focus();
  }

  private hideBroadcastPanel() {
    this.bcastPanel?.classList.add("hidden");
    for (const t of this.tiles.values()) t.bcastCheck.checked = false;
  }
```

Импортировать `broadcastInput` из `./broadcast` в начале файла.

- [ ] **Step 4: Регистрация команды broadcast в `main.ts`**

В `paletteCommands()` добавить пункт:

```ts
    { id: "broadcast", title: "Режим broadcast (ввод в несколько сессий)", run: () => deck.toggleBroadcast() },
```

В объект `COMMANDS` добавить:

```ts
  "broadcast": () => deck.toggleBroadcast(),
```

(Хоткей `b` → `broadcast` уже в `matchHotkey` из Task 11; диспетчеризация в `main.ts` идёт через `COMMANDS[id]`.)

- [ ] **Step 5: Стили в `styles.css`**

```css
.bcast-check.hidden { display: none; }
.bcast-check { margin-right: 6px; accent-color: var(--accent, #61afef); }
.bcast-panel {
  position: sticky; bottom: 0; display: flex; padding: 8px;
  background: var(--panel, #23262b); border-top: 1px solid var(--border, #333);
}
.bcast-panel.hidden { display: none; }
.bcast-input {
  flex: 1; padding: 6px 10px; border-radius: 6px;
  background: #1d1f21; color: #e6e6e6; border: 1px solid var(--border, #333);
}
```
(Проверить фактические имена токенов в `:root` в `styles.css`; при отсутствии — использовать существующие значения цветов терминала, как в примере.)

- [ ] **Step 6: Тесты и типы зелёные**

Run: `npm test` → PASS
Run: `npx tsc --noEmit` → без ошибок

- [ ] **Step 7: Ручная проверка (десктоп, чеклист)**

Cmd/Ctrl+B (или палитра) → на плитках появились чекбоксы + снизу поле. Отметить 2 плитки, ввести текст, Enter → ввод ушёл в обе. Повторный Cmd/Ctrl+B / Esc → режим выключен, чекбоксы скрыты. Обычный ввод в отдельный терминал не затронут.

- [ ] **Step 8: Commit**

```bash
git add src/sessions.ts src/main.ts src/styles.css tests/sessions-broadcast.test.ts index.html
git commit -m "feat: broadcast mode UI — tile checkboxes, input panel, command registration"
```

---

## Task 13: Финальное whole-branch ревью + документация

**Files:**
- Modify: `docs/superpowers/plans/RELEASE-1.0-CHECKLIST.md` (добавить раздел «Группа B» с ручными проверками из задач 6/10/12 и результатом спайка)

- [ ] **Step 1: Полный прогон**

Run: `npm test` → все зелёные
Run: `cargo test --manifest-path src-tauri/Cargo.toml` → все зелёные
Run: `npx tsc --noEmit` → чисто

- [ ] **Step 2: Whole-branch ревью**

Использовать `superpowers:requesting-code-review` (opus) по всему диффу `feat/group-b` против `main`. Особое внимание: (а) `--session-id`/`--resume` argv-порядок; (б) сериализация `SessionEntry` (serde rename `sessionId`); (в) фан-аут broadcast (CR, пустой набор); (г) отсутствие воровства ввода у xterm (хоткей `b` только под app-модификатором); (д) `persistLayout` не роняет UI при ошибке IPC.

- [ ] **Step 3: Дописать чеклист ручной приёмки**

Добавить в `RELEASE-1.0-CHECKLIST.md` раздел «Группа B»: continue при ⟳, плейсхолдеры, авто-restore, broadcast; и строку результата спайка (OK/DEGRADED).

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/plans/RELEASE-1.0-CHECKLIST.md
git commit -m "docs: Group B manual acceptance checklist + spike result"
```

- [ ] **Step 5: Готовность к PR**

Ветка `feat/group-b` готова к PR в `main` (как Release 1.0 / Группа A через PR). Финальный мерж — по решению владельца (`superpowers:finishing-a-development-branch`).

---

## Self-Review (заполняется автором плана)

**Spec coverage:**
- Фича 1 (continue) → Task 2 (Rust) + Task 3 (TS). Спайк `--settings`+`--resume` → Task 1. ✓
- Фича 2 (плейсхолдеры) → Task 4 (парсинг) + Task 5 (форма) + Task 6 (склейка). ✓
- Фича 3 (restore) → Task 7 (store) + Task 8 (команды) + Task 9 (IPC/сериализатор) + Task 10 (persist+restore). ✓
- Фича 4 (broadcast) → Task 11 (фан-аут/хоткей/команда) + Task 12 (UI). ✓
- Cross-cutting (реестр команд, capabilities, уроки vitest, последовательность) → Task 8 (примечание capabilities), Task 11 (реестр), Task 13 (ревью). ✓

**Type consistency:** `SessionEntry` (`sessionId`/`cwd`/`name`) одинаков в Rust (serde rename) и TS; `startSession(...,resume)` совпадает в ipc.ts и команде; `spawnTile` opts согласованы между `launch`/`restore`. ✓

**Placeholder scan:** нет TBD/«add error handling» — весь код приведён. Зависимость порядка Task 11→12: Task 11 даёт `broadcastInput` + хоткей, Task 12 реализует `toggleBroadcast` и регистрирует команду в `main.ts` — без заглушек, каждый коммит самодостаточен. ✓
</content>
</invoke>
